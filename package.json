{
  "name": "dotenv-manipulator",
  "version": "1.0.6",
  "description": "easily manipulate both dotenv (file) and process.env (variable)",
  "main": "index.js",
  "scripts": {
    "test": "touch .env && mocha test.js"
  },
  "author": "JiPai",
  "license": "GPL-3.0",
  "dependencies": {
    "dotenv": "^8.0.0",
    "dotenv-expand": "^5.1.0"
  },
  "repository": "github:JiPaix/dotenv-manipulator",
  "devDependencies": {
    "mocha": "^6.1.4"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}

{
  "name": "dotenv-manipulator",
  "version": "1.0.0",
  "description": "easlity manipulate both dotenv (file) and process.env (variable)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JiPai",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.0.0",
    "dotenv-expand": "^5.1.0"
  }
}

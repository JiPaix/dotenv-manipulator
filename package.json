{
  "name": "dotenv-manipulator",
  "version": "1.1.0",
  "description": "dotenv-manipulator allows you to add, remove or update both environment variables and .env files on runtime",
  "main": "index.js",
  "scripts": {
    "test": "rm -rf ./.env && touch ./.env && mocha test.js"
  },
  "author": "JiPai",
  "license": "GPL-3.0",
  "dependencies": {
    "dotenv": "^8.0.0",
    "dotenv-expand": "^5.1.0"
  },
  "repository": "github:JiPaix/dotenv-manipulator",
  "devDependencies": {
    "mocha": "^6.1.4"
  },
  "engines": {
    "node": ">=6.0.0",
    "npm": ">=3.8.6"
  },
  "keywords": [
    "dotenv",
    "environment",
    "manipulate",
    "slackers"
  ]
}
